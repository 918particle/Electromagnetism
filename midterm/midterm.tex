\title{Midterm for Electromagnetic Theory (PHYS330)}
\author{Dr. Jordan Hanson - Whittier College Dept. of Physics and Astronomy}
\date{\today}
\documentclass[10pt]{article}
\usepackage[a4paper, total={18cm, 27cm}]{geometry}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{hyperref}
\begin{document}
\twocolumn
\maketitle

\begin{abstract}
This exam may be completed at home, and covers chapters 1-4 of the course text and in-class examples.  Class notes and the course text may be used for review.  The course warm-up exercises are good study materials for this exam.
\end{abstract}
\noindent

\section{Chapter 1}

\begin{enumerate}
\item (a) If $\mathbf{A}$ and $\mathbf{B}$ are two vector functions, what does the expression $(\mathbf{A} \cdot \nabla) \mathbf{B}$ mean?  Derive $(\hat{r} \cdot \nabla) \hat{r}$, where $\hat{r}$ is $\mathbf{r}/r$. (b) One can show that the \textit{force} on a dipole moment $\mathbf{p}$ induced by a non-uniform field is
\begin{equation}
\mathbf{F} = (\mathbf{p} \cdot \nabla) \mathbf{E}
\end{equation}
Compute the force on a physical dipole located at the origin with $\mathbf{p} = q \mathbf{d}$ in a field with associated potential $V(\mathbf{r}) = V_0 r^2 + V_1$.
\item Evaluate the following integral using (a) the three-dimensional Dirac delta function, or (b) integration by parts.  Solving both earns a bonus point.
\begin{equation}
J = \int_{\mathcal{V}} e^{-r} \left( \nabla \cdot \frac{\mathbf{\hat{r}}}{r^2} \right)
\end{equation}
\end{enumerate}

\section{Chapter 2}

\begin{enumerate}
\item Suppose two dipoles, each with dipole moment $\mathbf{p}$ pointed in opposite directions, form a square with alternating positive and negative charges and side length $d$.  Calculate the field $\mathbf{E}_{\rm tot}$ at the following points $P$: (a) $P = (0,0)$, (b) $P = (2d,0)$, and $P = (0,2d)$.  Check units and take limits\footnote{This object is an electrostatic quadrupole.}.
\item The electric potential of some configuration is given by the expression
\begin{equation}
V(\mathbf{r}) = A \frac{e^{-\lambda r}}{r} \label{eq:lambda}
\end{equation}
In Eq. \ref{eq:lambda}, $A$ and $\lambda$ are constants.  Find the field $\mathbf{E}(\mathbf{r})$, the charge density $\rho$ and the total charge $Q$ in terms of $A$ and $\lambda$.  \textit{Hint: $\rho = \epsilon_0 A(4\pi\delta^3(\mathbf{r}) - \lambda^2\exp(-\lambda r)/r)$.} \textbf{Bonus:} compute the total energy stored in the field over all space.
\item (a) Use Gauss' Law to compute the field $\mathbf{E}$ as a function of the distance $s$ from a long straight wire with positive charge density $\lambda$. (b) Calculate the position versus time of a positive point charge $q$ with mass $m$ if it is released a distance $s$ from the wire.
\end{enumerate}

\section{Chapter 3}

\begin{enumerate}
\item Suppose the potential $V_0(\theta)$ at the surface of a sphere of radius $R$ is specified, and there is no charge inside or outside the sphere.  (a) Show that the charge density on the sphere is given by
\begin{align}
\sigma(\theta) &= \frac{\epsilon_0}{2R} \sum_{l=0}^{\infty} (2l+1)^2 C_l P_l(\cos\theta) \\
C_l &= \int_0^{\pi} V_0(\theta) P_l (\cos\theta) \sin\theta d\theta
\end{align}
(b) Produce the specific result for $\sigma(\theta)$ with $V_0(\theta) = P_2(\cos\theta)$.
\item A rectangular pipe, running parallel to the z-axis (from $-\infty$ to $\infty$), has three grounded metal sides, at $y=0$, $y=a$, and $x=0$.  The fourth side, $x=b$, is maintained at a potential $V_0(y)$. (a) Develop a general formula for the potential inside the pipe. (b) Find the potential explicitly, for the case $V_0(y) = V_0$.
\item Suppose a sphere has a uniform polarization $\mathbf{P}$.  What are the (a) $\sigma_{\rm b}$ and (b) $\rho_{\rm b}$? (c) Which Legendre polynomial does the charge distribution resemble? (d) Calculate the dipole moment of the system.  (e) Use the multipole expansion to approximate the voltage far from the sphere.  (f) If the system is placed in an external field $\mathbf{E} = E_0 \hat{\mathbf{x}}$, what is the torque?
\end{enumerate}

\section{Chapter 4}

\begin{enumerate}
\item (a) Derive the electric field inside a capacitor with surface charge density $\pm \sigma$, assuming the separation between the conductors is small compared to the area.  Derive the field for the same system, but with dielectric material between the plates.  The bulk polarization of the material is $\mathbf{P} = k\mathbf{E}$.
\end{enumerate}

\end{document}
